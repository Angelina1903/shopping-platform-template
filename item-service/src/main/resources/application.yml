server:
  port: 9002

spring:
  application:
    name: item-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/itemdb

management:
  endpoints:
    web:
      exposure:
        include: health,info